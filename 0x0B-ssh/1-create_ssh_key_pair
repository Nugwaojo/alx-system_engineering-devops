#!/usr/bin/env bash
# Script to generate an RSA key pair with specific parameters

# Define variables
KEY_NAME="school"
BITS=4096
PASSPHRASE="icequeen21"

# Generate the key pair
ssh-keygen -t rsa -b $BITS -f "$KEY_NAME" -N "$PASSPHRASE"

# Output success message
echo "RSA key pair generated successfully."
